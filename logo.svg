<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 280 48" fill="none">
  <!-- Icon: Stacked rectangles -->
  <g fill="currentColor">
    <!-- Top row: two small squares -->
    <rect x="0" y="0" width="10" height="10" rx="1"/>
    <rect x="12" y="0" width="10" height="10" rx="1"/>
    <!-- Bottom larger square with rounded corner extension -->
    <rect x="0" y="14" width="22" height="22" rx="2"/>
    <!-- Curved extension on bottom right -->
    <path d="M22 28 Q22 36 30 36 L30 28 Q30 20 22 20 L22 28 Z"/>
  </g>
  <!-- SpillStack text -->
  <text x="46" y="34" font-family="system-ui, -apple-system, BlinkMacSystemFont, sans-serif" font-size="32" font-weight="400" fill="currentColor" letter-spacing="-0.02em">SpillStack</text>
</svg>
